import React from 'react';
import { FormikResetForm } from '../../common/components/auth/reset-form';

const ResetPassPageContainer = () => <FormikResetForm />;

export default ResetPassPageContainer;
